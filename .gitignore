node_modules
dist
.npm-cache
.vercel

# Local git metadata when using separate-git-dir (see .git file)
.git-repo

# Full desktop export (large); regenerate with rsync locally if needed
desktop-copy-export/
